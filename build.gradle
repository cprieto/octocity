allprojects {
    apply plugin: 'java'

    sourceCompatibility = 1.6
    
    repositories {
        mavenCentral()
        jcenter()
    }

    jar {
        appendix = "${rootProject.name}"
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.3'
}
